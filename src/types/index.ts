export type Option = {
  id: string;
  label: string;
};

export enum Profiles {
  employee = "employee",
  applicant = "applicant",
  professor = "professor",
  student = "student",
  pupil = "pupil",
  partner = "partner",
  event_participant = "event_participant",
  graduate = "graduate",
  supervisor = "supervisor",
}
